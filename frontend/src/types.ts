export type StageRow = { stage: number; count: number; share: number; ecl: number };
export type PortfolioSummary = { total_ecl: number; total_ead: number; provision_pct: number; avg_pd: number; n: number; stage_mix: StageRow[]; ecl_by_region: { region: string; ecl: number }[]; monthly: { report_month: string; ecl: number; stage_1_share: number; stage_2_share: number; stage_3_share: number }[]; stage_rule_agreement_pct: number; lgd_used: number; lgd_source: string };
export type ScenarioResult = { scenario_ecl: Record<string, number>; weighted_ecl: number; weights: Record<string, number> };
export type Borrower = { identity: Record<string, string | number>; stage: number; stage_rule: number; pd_12m: number; pd_lifetime: number; lgd_used: number; ead_used: number; ecl: number; factor_context: string[] };
export type LgdSummary = { portfolio_lgd: { n: number; total_ead: number; mean_lgd: number; ead_weighted_lgd: number; mean_recovery: number }; segments: Record<string, { count: number; total_ead: number; mean_lgd: number; ead_weighted_lgd: number }[]>; ltv_effect: { ltv_bucket: string; mean_lgd: number; count: number }[] | null; collateral_effect: Record<string, Record<string, number>> | null; model_accuracy: { mae: number; rmse: number; bias: number; corr: number; scatter_obs: number[]; scatter_pred: number[] } | null; downturn_lgd: { uplift: number; stressed_weighted_lgd: number }; lgd_for_ecl: number };
